node_modules
# The server's SQLite dir at the repo root — anchored so it doesn't also swallow
# nested source dirs named `data` (e.g. the Android app's pl.filmowo.data package).
/data/
# Generated by `npm run build` (esbuild); rebuilt in CI/Docker, never committed.
public/dist/
.env
.env.local
.env.*
*.bak*
.claude/
.DS_Store

# Test artifacts
test-results/
playwright-report/
blob-report/
.playwright/
